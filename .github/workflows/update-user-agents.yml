---
name: Update User Agents

on:
  workflow_dispatch:
  schedule:
    - cron: 0 6 1 */4 *

permissions: read-all

jobs:
  open-pr:
    permissions:
      pull-requests: write
      contents: write
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6.0.2 # zizmor: ignore[unpinned-uses]
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: update the user agents list
        run: make update.user.agent

      - name: Lint Code Base to fmt the user agent json
        run: make lint

      - name: Open Pull Request
        if: ${{ !env.ACT }}
        uses: peter-evans/create-pull-request@v8 # zizmor: ignore[unpinned-uses]
        with:
          title: "chore: update user agents"
          body: "Update `agents.json`."
          commit-message: Update agents.json
          assignees: "giglium"
          labels: "feature"
          token: ${{ secrets.GITHUB_TOKEN }}
